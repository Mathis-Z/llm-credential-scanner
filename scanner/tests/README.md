# Testing Setup

(Work in progress)

Running the tests will prune all containers and networks!

Use `python3 -m scanner.tests.tests --keep` to run the test and keep the DB files produced. Each app gets its own DB in /tmp.
